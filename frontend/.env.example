# Backend API URL — set this when the PIA backend runs on a separate server
# Default (local dev): http://127.0.0.1:8000
NEXT_PUBLIC_API_URL=http://127.0.0.1:8000

# WebSocket URL for live dashboard streaming
# Default (local dev): ws://127.0.0.1:8000/ws
NEXT_PUBLIC_WS_URL=ws://127.0.0.1:8000/ws
